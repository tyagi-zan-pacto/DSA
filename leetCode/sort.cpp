#include <bits/stdc++.h>
using namespace std;
int main(){
    stack<int> s;
    vector<int> v;
    int N = 5;
    int arr[]= {0, 2, 1, 2, 0};
    for (int i = 0; i < N; i++)
    {
        if(s.size()==0){
            v.push_back(-1);
        }
        else if(s.size()>0 and s.top()>)
    }
    
    return 0;
}